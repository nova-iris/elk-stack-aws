---
# Elasticsearch 8.x specific settings

# Security settings for 8.x
elasticsearch_security_enabled: true
elasticsearch_xpack_security: true

# TLS/SSL is mandatory in 8.x with security enabled
elasticsearch_ssl_enabled: true

# Auto-configuration is the default for 8.x first install
elasticsearch_auto_configuration: true